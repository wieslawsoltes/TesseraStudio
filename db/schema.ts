import {sqliteTable,text,integer,index,uniqueIndex,primaryKey} from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),blob:text('blob').notNull(),created:integer('created').notNull(),updated:integer('updated').notNull()});
export const members=sqliteTable('members',{projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),userId:text('user_id').notNull(),name:text('name').notNull(),role:text('role').notNull()},t=>[primaryKey({columns:[t.projectId,t.userId]}),index('members_user').on(t.userId)]);
export const operations=sqliteTable('operations',{seq:integer('seq').primaryKey({autoIncrement:true}),projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),opId:text('op_id').notNull(),actor:text('actor').notNull(),payload:text('payload').notNull(),created:integer('created').notNull()},t=>[uniqueIndex('operations_project_id').on(t.projectId,t.opId),index('operations_project_seq').on(t.projectId,t.seq)]);
export const comments=sqliteTable('comments',{id:text('id').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),userId:text('user_id').notNull(),name:text('name').notNull(),text:text('text').notNull(),channel:text('channel'),tile:integer('tile'),created:integer('created').notNull()},t=>[index('comments_project_created').on(t.projectId,t.created)]);
export const invites=sqliteTable('invites',{hash:text('hash').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),role:text('role').notNull(),expires:integer('expires').notNull(),created:integer('created').notNull()},t=>[index('invites_project').on(t.projectId)]);
